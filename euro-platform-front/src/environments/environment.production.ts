// Production build (Vercel `main` branch) -- not wired to a real deployment yet. Fill this
// in with the production euro-platform-api URL when the prod environment is created.
export const environment = {
  apiBaseUrl: 'https://REPLACE_WITH_PROD_RAILWAY_API_URL',
};
