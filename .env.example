# Postgres (shared instance, one schema per backend service)
POSTGRES_USER=euro
POSTGRES_PASSWORD=changeme
POSTGRES_DB=euro_platform
POSTGRES_PORT=5432

# euro-auth
JWT_SECRET=changeme-super-secret

# euro-platform-api -- CarAPI (carapi.app) credentials for the vehicle catalog.
# Sign up on their dashboard to get these; free tier covers 2015-2020 model years.
CARAPI_API_TOKEN=
CARAPI_API_SECRET=

#  -- Google Cloud Storage --
GCS_BUCKET_NAME=
GCS_PROJECT_ID=
# Place the downloaded service-account JSON key at euro-platform-api/secrets/gcs-service-account.json
# (gitignored) -- docker-compose mounts it into the container; do not put its contents here.

# Copy this file to .env and fill in real values before running docker compose.
# AUTH_SERVICE_URL is not listed here: it's set inside docker-compose.yml since
# it points at the internal Docker network hostname (http://euro-auth:3000),
# not something you configure per environment locally.
